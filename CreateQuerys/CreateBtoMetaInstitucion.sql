create table BTO_META_INSTITUCION(
    CLAVE NUMBER GENERATED BY DEFAULT AS IDENTITY,
    CURSO INTEGER NOT NULL,
    CODIGO_COLEGIO VARCHAR(30) not null,
    SUBNIVEL_META VARCHAR(10) not null,
    SUBNIVEL_INICIAL VARCHAR(10) not null,--FILL BY INSTITUE
    META VARCHAR(10) default 'NOLVL',--APP COURSE GOAL LEVEL
    HORAS_SEMANAL numeric default 0,--FILL BY INSTITUE
    PRIMARY KEY (CLAVE)
);

ALTER TABLE BTO_META_INSTITUCION add CONSTRAINT BTO_META_INSTITUCION_UNIQUES UNIQUE (CURSO, CODIGO_COLEGIO);
commit;
